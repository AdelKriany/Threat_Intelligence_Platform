services:
  postgres:
    image: postgres:16-alpine
    container_name: threatlens-postgres
    environment:
      POSTGRES_DB: threatlens
      POSTGRES_USER: threatlens
      POSTGRES_PASSWORD: threatlens
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U threatlens -d threatlens || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: threatlens-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep -q PONG || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: threatlens-api
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://threatlens:threatlens@postgres:5432/threatlens}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      CELERY_BROKER_URL: ${REDIS_URL:-redis://redis:6379/0}
      CELERY_RESULT_BACKEND: ${REDIS_URL:-redis://redis:6379/0}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ENRICHMENT_ENABLED: ${ENRICHMENT_ENABLED:-false}
      ENRICHMENT_TTL_SECONDS: ${ENRICHMENT_TTL_SECONDS:-86400}
      ENRICHMENT_REQUEST_TIMEOUT_SECONDS: ${ENRICHMENT_REQUEST_TIMEOUT_SECONDS:-10}
      ENRICHMENT_MAX_RETRIES: ${ENRICHMENT_MAX_RETRIES:-2}
      ENRICHMENT_BATCH_SIZE: ${ENRICHMENT_BATCH_SIZE:-100}
      ENRICHMENT_REFRESH_INTERVAL_MINUTES: ${ENRICHMENT_REFRESH_INTERVAL_MINUTES:-60}
      ENRICHMENT_RATE_LIMIT_RETRY_SECONDS: ${ENRICHMENT_RATE_LIMIT_RETRY_SECONDS:-900}
      ENRICHMENT_NOT_FOUND_TTL_SECONDS: ${ENRICHMENT_NOT_FOUND_TTL_SECONDS:-21600}
      ENRICHMENT_FAILURE_RETRY_SECONDS: ${ENRICHMENT_FAILURE_RETRY_SECONDS:-900}
      ENRICHMENT_RETRY_MAX_DELAY_SECONDS: ${ENRICHMENT_RETRY_MAX_DELAY_SECONDS:-60}
      NVD_ENABLED: ${NVD_ENABLED:-true}
      NVD_API_KEY: ${NVD_API_KEY:-}
      CISA_KEV_ENABLED: ${CISA_KEV_ENABLED:-true}
      CISA_KEV_CATALOG_URL: ${CISA_KEV_CATALOG_URL:-https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json}
      CISA_KEV_TTL_SECONDS: ${CISA_KEV_TTL_SECONDS:-21600}
      CISA_KEV_REFRESH_INTERVAL_MINUTES: ${CISA_KEV_REFRESH_INTERVAL_MINUTES:-360}
      EPSS_ENABLED: ${EPSS_ENABLED:-true}
      EPSS_API_URL: ${EPSS_API_URL:-https://api.first.org/data/v1/epss}
      EPSS_BATCH_SIZE: ${EPSS_BATCH_SIZE:-100}
      EPSS_TTL_SECONDS: ${EPSS_TTL_SECONDS:-86400}
      EPSS_REFRESH_INTERVAL_MINUTES: ${EPSS_REFRESH_INTERVAL_MINUTES:-1440}
      ABUSEIPDB_ENABLED: ${ABUSEIPDB_ENABLED:-false}
      ABUSEIPDB_API_KEY: ${ABUSEIPDB_API_KEY:-}
      VIRUSTOTAL_ENABLED: ${VIRUSTOTAL_ENABLED:-false}
      VIRUSTOTAL_API_KEY: ${VIRUSTOTAL_API_KEY:-}
      PYTHONPATH: /app/backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend:z
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: ["uvicorn", "app.main:create_app", "--host", "0.0.0.0", "--port", "8000", "--factory"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0) if urllib.request.urlopen('http://127.0.0.1:8000/api/v1/health').status==200 else sys.exit(1)\" || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  celery-worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: threatlens-celery-worker
    command: ["python", "/app/scripts/celery_wrapper.py", "-A", "app.workers.celery_app", "worker", "--loglevel=info"]
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://threatlens:threatlens@postgres:5432/threatlens}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      CELERY_BROKER_URL: ${REDIS_URL:-redis://redis:6379/0}
      CELERY_RESULT_BACKEND: ${REDIS_URL:-redis://redis:6379/0}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ENRICHMENT_ENABLED: ${ENRICHMENT_ENABLED:-false}
      ENRICHMENT_TTL_SECONDS: ${ENRICHMENT_TTL_SECONDS:-86400}
      ENRICHMENT_REQUEST_TIMEOUT_SECONDS: ${ENRICHMENT_REQUEST_TIMEOUT_SECONDS:-10}
      ENRICHMENT_MAX_RETRIES: ${ENRICHMENT_MAX_RETRIES:-2}
      ENRICHMENT_BATCH_SIZE: ${ENRICHMENT_BATCH_SIZE:-100}
      ENRICHMENT_REFRESH_INTERVAL_MINUTES: ${ENRICHMENT_REFRESH_INTERVAL_MINUTES:-60}
      ENRICHMENT_RATE_LIMIT_RETRY_SECONDS: ${ENRICHMENT_RATE_LIMIT_RETRY_SECONDS:-900}
      ENRICHMENT_NOT_FOUND_TTL_SECONDS: ${ENRICHMENT_NOT_FOUND_TTL_SECONDS:-21600}
      ENRICHMENT_FAILURE_RETRY_SECONDS: ${ENRICHMENT_FAILURE_RETRY_SECONDS:-900}
      ENRICHMENT_RETRY_MAX_DELAY_SECONDS: ${ENRICHMENT_RETRY_MAX_DELAY_SECONDS:-60}
      NVD_ENABLED: ${NVD_ENABLED:-true}
      NVD_API_KEY: ${NVD_API_KEY:-}
      CISA_KEV_ENABLED: ${CISA_KEV_ENABLED:-true}
      CISA_KEV_CATALOG_URL: ${CISA_KEV_CATALOG_URL:-https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json}
      CISA_KEV_TTL_SECONDS: ${CISA_KEV_TTL_SECONDS:-21600}
      CISA_KEV_REFRESH_INTERVAL_MINUTES: ${CISA_KEV_REFRESH_INTERVAL_MINUTES:-360}
      EPSS_ENABLED: ${EPSS_ENABLED:-true}
      EPSS_API_URL: ${EPSS_API_URL:-https://api.first.org/data/v1/epss}
      EPSS_BATCH_SIZE: ${EPSS_BATCH_SIZE:-100}
      EPSS_TTL_SECONDS: ${EPSS_TTL_SECONDS:-86400}
      EPSS_REFRESH_INTERVAL_MINUTES: ${EPSS_REFRESH_INTERVAL_MINUTES:-1440}
      ABUSEIPDB_ENABLED: ${ABUSEIPDB_ENABLED:-false}
      ABUSEIPDB_API_KEY: ${ABUSEIPDB_API_KEY:-}
      VIRUSTOTAL_ENABLED: ${VIRUSTOTAL_ENABLED:-false}
      VIRUSTOTAL_API_KEY: ${VIRUSTOTAL_API_KEY:-}
      PYTHONPATH: /app/backend
    volumes:
     - ./backend:/app/backend:z
    depends_on:
     postgres:
       condition: service_healthy
     redis:
       condition: service_healthy
    restart: unless-stopped

  celery-beat:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: threatlens-celery-beat
    command: ["python", "/app/scripts/celery_wrapper.py", "-A", "app.workers.celery_app", "beat", "--loglevel=info", "-s", "/tmp/celerybeat-schedule"]
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-development}
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://threatlens:threatlens@postgres:5432/threatlens}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      CELERY_BROKER_URL: ${REDIS_URL:-redis://redis:6379/0}
      CELERY_RESULT_BACKEND: ${REDIS_URL:-redis://redis:6379/0}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      ENRICHMENT_ENABLED: ${ENRICHMENT_ENABLED:-false}
      ENRICHMENT_TTL_SECONDS: ${ENRICHMENT_TTL_SECONDS:-86400}
      ENRICHMENT_REQUEST_TIMEOUT_SECONDS: ${ENRICHMENT_REQUEST_TIMEOUT_SECONDS:-10}
      ENRICHMENT_MAX_RETRIES: ${ENRICHMENT_MAX_RETRIES:-2}
      ENRICHMENT_BATCH_SIZE: ${ENRICHMENT_BATCH_SIZE:-100}
      ENRICHMENT_REFRESH_INTERVAL_MINUTES: ${ENRICHMENT_REFRESH_INTERVAL_MINUTES:-60}
      ENRICHMENT_RATE_LIMIT_RETRY_SECONDS: ${ENRICHMENT_RATE_LIMIT_RETRY_SECONDS:-900}
      ENRICHMENT_NOT_FOUND_TTL_SECONDS: ${ENRICHMENT_NOT_FOUND_TTL_SECONDS:-21600}
      ENRICHMENT_FAILURE_RETRY_SECONDS: ${ENRICHMENT_FAILURE_RETRY_SECONDS:-900}
      ENRICHMENT_RETRY_MAX_DELAY_SECONDS: ${ENRICHMENT_RETRY_MAX_DELAY_SECONDS:-60}
      NVD_ENABLED: ${NVD_ENABLED:-true}
      NVD_API_KEY: ${NVD_API_KEY:-}
      CISA_KEV_ENABLED: ${CISA_KEV_ENABLED:-true}
      CISA_KEV_CATALOG_URL: ${CISA_KEV_CATALOG_URL:-https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json}
      CISA_KEV_TTL_SECONDS: ${CISA_KEV_TTL_SECONDS:-21600}
      CISA_KEV_REFRESH_INTERVAL_MINUTES: ${CISA_KEV_REFRESH_INTERVAL_MINUTES:-360}
      EPSS_ENABLED: ${EPSS_ENABLED:-true}
      EPSS_API_URL: ${EPSS_API_URL:-https://api.first.org/data/v1/epss}
      EPSS_BATCH_SIZE: ${EPSS_BATCH_SIZE:-100}
      EPSS_TTL_SECONDS: ${EPSS_TTL_SECONDS:-86400}
      EPSS_REFRESH_INTERVAL_MINUTES: ${EPSS_REFRESH_INTERVAL_MINUTES:-1440}
      ABUSEIPDB_ENABLED: ${ABUSEIPDB_ENABLED:-false}
      ABUSEIPDB_API_KEY: ${ABUSEIPDB_API_KEY:-}
      VIRUSTOTAL_ENABLED: ${VIRUSTOTAL_ENABLED:-false}
      VIRUSTOTAL_API_KEY: ${VIRUSTOTAL_API_KEY:-}
      PYTHONPATH: /app/backend
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
